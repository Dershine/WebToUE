你正在继续开发 Unreal Engine 5.8 插件 WebToUE。

项目根目录：
`E:\UEProjects\PersonalGame\WebToUE`

## 本窗口参数

- 目标：【默认 `AUTO`：按本文选择顺序完成一个可独立验收的最小工程闭环；也可填写指定任务。】
- 范围：【默认一个微观工作包，或其中一个可测试、可交付的最小纵向闭环；不无依据跨越多个架构阶段。】
- 执行模式：【默认 `PLAN_ONLY`：只读调查并输出计划，等我回复“开始实施”后再修改；若本请求已明确包含“开始实施”，则直接进入 `IMPLEMENT`。】
- Git 权限：【`IMPLEMENT` 可修改范围内项目文件，但不得自动 stage、commit 或 push。只有我明确要求“提交”才允许审计后暂存并提交；只有明确要求“推送”才允许 push。】

## 一、事实源、权威边界与状态恢复

遵守当前会话的系统/开发者指令、用户本次要求、根目录 `AGENTS.md` 和任务触发的 Skill。本提示词的 Git 权限覆盖 `AGENTS.md` 中一般性的 milestone commit 建议：未经明确要求，不得 stage、commit 或 push。

仓库事实按以下职责读取，不在本提示词缓存路线编号、预算值或当前测试数：

- `CONTEXT.md`：稳定的统一领域语言。
- `Plugins/WebToUE/Docs/WTUE_TechnicalSummary.md`：当前架构、工程判断、性能合同、预算、风险、路线和门禁。
- `Plugins/WebToUE/Docs/WTUE_EvidenceLedger.md`：带时间语境的验证、性能基线、构建与重要工程变更。
- `Plugins/WebToUE/Docs/WTUE_SupportMatrix.md`：HTML/CSS/绑定/输入/资源/诊断的精确当前边界。
- `Plugins/WebToUE/Docs/ADRs/`：已作出的长期架构决策。
- `AGENTS.md`：工具路由、安全边界、VibeUE/UE MCP 规则和项目 gotcha。
- `$operate-webtoue-editor`：Editor 生命周期、构建重启、readiness、MCP、Automation、视觉证据和 tracked BuildCookRun 的唯一具体流程。

三层权威关系：

- Technical Summary 决定“做什么、为什么做、通过标准是什么”。
- `$operate-webtoue-editor` 决定“如何安全操作 Editor 和执行其覆盖的门禁”。
- 测试结果、构建退出码、Trace、日志与可复现 Corpus 决定“实际上是否通过”；附近但不同的门禁不能替代所需证据。

开始前：

1. 完整阅读 `CONTEXT.md`。
2. 固定检查分支、HEAD、`git status`、暂存区、未提交差异、未跟踪文件和最近相关提交。
3. 阅读 Technical Summary 的当前仪表盘、风险、活跃里程碑、相关微观路线和测试/发布门禁。路线选择、跨章节状态审计或事实冲突时完整阅读。
4. 按任务读取其他事实源：能力边界读 Support Matrix；性能对比、历史验证或构建发布读 Evidence Ledger；长期决策读相关 ADR。
5. 定位任务相关的实现、所有权、失效边界、调用链、测试入口和相关历史；不适用的调查不强行展开。
6. 输出被截断或分页时继续读取到所需内容完整结束。

现有修改可能属于我或上一窗口。必须先理解并保留，不得 reset、checkout 覆盖、clean、删除、顺手格式化或将未跟踪文件当作垃圾；无关修改不得进入本次交付或暂存。Dirty 文件本身不等于仍有未完成工作，必须结合差异、证据和退出条件判断。

## 二、`AUTO` 选择算法

目标为 `AUTO` 时，按以下顺序选择一个最小闭环：

1. 用户本窗口明确指定的任务。
2. 有明确未完成意图、且与当前目标或活跃工作包相关的已有未提交/未跟踪工作。
3. 已开始但尚未满足退出条件的最小闭环。
4. 活跃里程碑中依赖已满足、优先级最高的第一个未完成微观工作包；工作包过大时选择其中能独立证明价值的最小纵向切片。

选择前确认里程碑、微观工作包、前置依赖、风险编号、可复现证据和退出条件。严格遵循 Technical Summary 当前记录的依赖顺序、首个未满足验收项和 Go/No-Go 条件，不使用本提示词中的旧阶段记忆。优先收口相关半成品，不同时开启新工作包；只有继续会显著偏离用户目标或存在不安全歧义时才请求决定。

文档与源码、测试或实际构建冲突时，以可复现证据为准。冲突影响本次判断时，在交付范围内同步正确的事实源；不得用未验证的当前代码覆盖已有可靠证据。

## 三、执行模式与 Git 边界

### `PLAN_ONLY`

只进行非破坏性、只读调查，不修改文件、资产、Git 状态或其他外部状态。按适用性输出：

1. 问题、当前证据以及为什么现在应该做。
2. 所属里程碑、微观工作包、风险编号、前置依赖和退出条件。
3. 会修改的模块、资产、测试和文档边界，以及明确非范围。
4. 测试、构建、性能、Editor、Packaged Runtime 或发布验收方法。
5. 现有工作树修改如何保留或续接。

不适用项可省略或标记 N/A。计划必须形成有实际价值的最小闭环，不得只创建空接口、占位实现或 TODO。输出后停止，等待“开始实施”。只询问无法从仓库查明且会显著改变方案的关键歧义。

### `IMPLEMENT`

按已确认目标连续完成实现、验证和必要文档同步。范围内的常规决策自行做出。新证据推翻关键假设时报告并调整；只有需要新增授权、改变目标或明显扩大范围时才停止询问。

Git 规则：

- 允许读取状态/历史、修改范围内文件、执行非破坏性测试与构建。
- 禁止 reset、checkout 覆盖、clean、删除用户修改、修改无关文件或重写历史。
- “提交”只授权审计并显式暂存本次相关文件后 commit；不得宽泛暂存。提交后报告哈希和剩余工作树状态。
- “提交”不授权 push；“推送”不授权强制推送或重写历史。

## 四、不可违反的工程边界

- HTML/CSS 是 UI Source，不是 Runtime 中运行的网页。
- 不引入 CEF、Chromium、WebView、通用浏览器内核或承载 Runtime 状态/交互的通用 JavaScript VM。
- 保持 Compiled UI IR、Runtime State、Render Data 与 Layout/Paint/Resource Cache 生命周期分离。
- 每个 UI 节点不默认对应 UObject、UWidget 或独立 Slate Widget。
- 响应式数据优先使用 UObject、FieldNotify、UE MVVM 和类型化命令。
- 活跃里程碑的具体优先级、冻结范围和退出门以 Technical Summary 为准；不得绕过依赖顺序扩张大型 CSS、组件、动画或浏览器兼容能力。
- MCP/VibeUE 只是可选 Editor Automation Surface，不得成为 Core、Runtime、Cooked 游戏或产品协议依赖。
- VibeUE 和 Yoga 是第三方代码；任务未明确要求且缺少验证证据时不得修改。

实现必须先理解现有所有权、失效边界、测试和错误模型，沿用现有类型、命名和模块边界，然后修复根因。不扩大范围修复无关问题；可记录有证据的后续项。新增或改变行为时，添加能在修改前失败、修改后通过的自动化测试；中间模型正确不能代替最终渲染、序列化、Cook 或用户可见结果正确。

保持 UE 5.8、Win64 和现有 Cook/资产版本边界。修改 Compiled UI IR 或序列化数据时，同时处理自定义版本、迁移/重编译策略、旧资产行为和失败诊断。新稳定公共术语进入 `CONTEXT.md`；存在真实替代方案、难以逆转且代码无法自证的决定创建 ADR。待裁决的实现名称或临时方案不得提前写成稳定领域语言。

性能敏感路径需说明调用范围、工作量、复杂度、缓存所有权、Dirty 传播、失效粒度、热路径分配和重复计算。对于 K=1 局部变化，同时报告总规模 N、访问/匹配/求值/重算节点数、布局/绘制工作量与资源加载次数；仅耗时下降但仍执行不必要的 O(N) 工作，不算满足增量运行时目标。

只有修改预算路径或宣称性能改善时，才必须提供同政策、同环境的可比前后数据；纯正确性修复不得凭代码形状或单次样本宣称性能改善。不得用 Editor 微基准、单阶段耗时或外部营销数字宣称产品性能对等。对等结论必须满足 Technical Summary 的同硬件、同画面、同输入轨迹、同视觉结果和 Release/Packaged 测量合同；缺少对方环境时标记 `Unknown`。

## 五、工具路由与风险验收

- C++、配置、文档、测试源码、静态分析、Git 和构建输出使用文件与命令行工具。
- `.uasset`、Blueprint、Material、Widget、关卡、PIE、Viewport、Editor 日志/Python、资产编译/重读、Unreal Automation Test 和 Runtime/World 证据使用 UE MCP/VibeUE。
- 不直接解析、编辑或覆盖 `.uasset`。Editor 调用前发现当前工具与精确 Schema；资产写入保持幂等，结构修改后编译，写入后重读验证。
- 同一时间只允许一个 Agent/窗口修改 UE 资产。
- 凡涉及 Editor 健康、PIE、保存资产、C++ 构建重启、readiness、MCP 健康、Editor 截图或 Unreal Automation Test，必须使用 `$operate-webtoue-editor`，以 Skill 为唯一具体流程来源。默认交付时保持 Editor 开启。
- BuildCookRun 成功只证明其记录的 Build/Cook/Stage/Pak/IoStore 与恢复门，不证明 staged executable 已运行，也不证明 Packaged Runtime、输入、渲染或性能验收。任务需要这些证据时必须运行对应 Harness；若当前没有安全 Harness，明确标记 `未验证` 并报告能力缺口。

### 验证分层与候选冻结

验证分为三个阶段：

1. 实施循环：每次修改只运行最接近失效边界的聚焦测试、静态检查和受影响模块的最小编译，不在源码仍持续变化时重复运行完整发布门。
2. 候选冻结：范围内源码、测试、资产和必要文档完成后，审计实际 diff 并声明候选冻结；随后运行完整 WebToUE 回归、要求的 Editor/运行时验收及 UE 5.8 Win64 Editor Development 编译。
3. 发布门：仅当修改影响 Cook、序列化、Runtime 依赖、模块加载、打包或发布边界，或者任务明确要求时，运行 Cook/Stage/Pak/IoStore。Shipping 仅在 Shipping 边界受影响或明确要求时运行；BuildPlugin 仅在插件分发、描述文件、模块装载、第三方依赖或打包结构变化时运行。

昂贵门禁失败后，先保存原始日志、退出码和仍存活的子进程证据，不得因宿主超时或输出安静而重复启动。若失败导致源码或资产修正，候选重新打开；先重跑受影响的廉价门，再重新冻结并执行必要的昂贵门。

验证强度按影响与风险选择：

- 所有修改：最接近边界的测试或静态验证、`git diff --check`、实际 diff、格式/引用和意外生成文件检查。
- C++/模块/行为修改：实施中运行聚焦测试和受影响模块的最小 Editor Development 编译；候选冻结后运行完整 WebToUE 回归和 UE 5.8 Win64 Editor Development 编译。只有涉及 Game receipt、`WITH_EDITOR` 边界或发布目标时才增加 Game 构建。
- 性能预算路径：固定 Benchmark Corpus、既有采样政策、可比前后数据、预算/回归幅度、工作量和瓶颈结论。产品级结论另需 Technical Summary 规定的 GT/RT/GPU、首帧、内存及端到端证据。
- Runtime/视觉/资产/序列化/模块加载/发布边界：按影响增加最终 Slate/Viewport/PIE、Game/Standalone、Packaged Runtime、Cook、IoStore、Shipping、BuildPlugin、旧资产迁移/重编译和资产编译状态证据。
- 纯文档：验证 Markdown/格式、链接、事实一致性和意外文件，不运行无意义的 Editor、编译或发布门。
- 配置：按配置的真实消费者选择验证；模块、Cook、AssetManager、插件描述或打包配置必须进入对应的编译、加载、Cook 或发布门，不能按纯文档处理。

视觉正确性不能只验证中间样式数据；应检查最终绘制输出，并在适合时建立离屏像素或 Screenshot/Golden 回归。任何未执行、失败或受环境限制的验证必须明确标记为“未验证”或“失败”；工具调用成功不等于工程验收成功。

## 六、长期状态与交付

完成实现和验证后，按实际变化更新对应的唯一事实源：

- 当前架构、能力判断、预算、风险、路线或门禁 → `WTUE_TechnicalSummary.md`。
- 新的历史验证、性能数据、构建/发布证据或会改变工程判断的变更 → `WTUE_EvidenceLedger.md`。
- HTML/CSS/绑定/输入/资源/诊断边界 → `WTUE_SupportMatrix.md`。
- 新稳定公共术语 → `CONTEXT.md`。
- 难以逆转且存在真实替代方案的决定 → ADR，并从 Technical Summary 链接。

本次未改变对应长期事实时，不制造无意义文档修改。路线进度按已满足验收项计算，标题、汇总和复选框数必须一致。微观工作包中的一个纵向切片完成，不等于整个工作包完成；只有该工作包的全部退出条件满足后才更新完成状态。只有实现、测试、文档和任务要求的性能/发布证据全部满足时才能勾选完成。

“最近核验”必须对应实际验证过的状态；未提交代码写作“当前 HEAD + working tree”，不伪造哈希。Evidence Ledger 保留当时事实，不用当前测试数、新 schema 或新口径反向改写历史记录。

只有退出条件和对应证据满足时才能宣布闭环完成。最终报告结论先行，并按适用性包含：

1. 实际工程结果和关键修改文件。
2. 关键设计取舍。
3. 已执行的测试、编译、Editor、视觉、性能、Packaged Runtime 和发布验证。
4. 更新的事实源、里程碑、验收项和风险。
5. 未解决问题、失败项和明确未验证项。
6. 当前 Git 状态。

仅当本次改变路线状态、目标为 `AUTO` 或我要求继续推进时，推荐下一个最小工作包。不重复过程记录，不把工具调用成功当作工程验收，不以“基本完成”掩盖未满足的退出条件。
